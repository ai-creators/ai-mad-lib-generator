export enum OpenaiLabel {
  PROMPT_EXCEEDS_MAX_LENGTH = 'Prompt cannot exceed the max length of 100 character.',
  ERROR_CREATING_ADLIB = 'Error creating adlib. Try changing the prompt for better results.',
}
