import { IsBeforeNowConstraint } from './is-before-now-constraint';

describe('IsBeforeNowConstraint', () => {
  it('should be defined', () => {
    expect(new IsBeforeNowConstraint()).toBeDefined();
  });
});
