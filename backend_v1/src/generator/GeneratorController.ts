import { NextFunction, Request, Response } from "express";

export class GeneratorController {
  public static generateRandomLib(
    req: Request,
    res: Response,
    next: NextFunction
  ) {}

  public static generateLib(req: Request, res: Response, next: NextFunction) {}
}
