import { NextFunction, Request, Response } from "express";
import { Controller } from "../../common/Controller";

export class AdLibController extends Controller {
  public getLibs(req: Request, res: Response, next: NextFunction) {}
}
