import { useEffect, useState } from "react";

const UserAdLibs = ({ user }) => {
  const [adlibs, setAdlibs] = useState;
  useEffect(() => {
    (async () => {})();
  }, []);
  return <div>UserAdLibs</div>;
};

export default UserAdLibs;
